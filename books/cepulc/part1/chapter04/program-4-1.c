#include <stdio.h>

int
main(void) {
    int soma = 0;

    soma = soma + 1 * 1;
    soma = soma + 2 * 2;
    soma = soma + 3 * 3;
    soma = soma + 4 * 4;

    printf("Soma dos primeiros 4 quadrados: %d\n", soma);

    return 0;
}
